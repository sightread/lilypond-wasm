/** Absolute path to `grace-free-midi.ily`: an `\includeSettings` recipe that renders a score's
 * MIDI with repeats unfolded and grace notes removed, discarding the source's own `\layout` and
 * `\midi` output. */
export const GRACE_FREE_MIDI_PATH: string
